import 'package:flutter/material.dart';

class LoginRegisterPage extends StatefulWidget {
  final void Function()? onTap;

  const LoginRegisterPage({super.key, required this.onTap});

  @override
  State<LoginRegisterPage> createState() => _LoginRegisterPage();
}

class _LoginRegisterPage extends State<LoginRegisterPage> {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class MyHomeComp extends StatelessWidget {
  const MyHomeComp({super.key});
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
